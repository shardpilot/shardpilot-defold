# Lane B occurrences per file, as of the commit that wrote this.
# Written by: scripts/check_public_surface.sh --write-baseline
#
# THIS FILE ONLY EVER SHRINKS. The scan fails when a count here rises,
# when a file appears that is not here, and when a number here is higher
# than the same number on the merge target. It also fails when a count
# FALLS without this file being updated, so paying debt is recorded
# rather than silently banked as slack.
#
# format-version: 3
# Format: <occurrences> <path> -- count FIRST so a path may contain spaces
#
# format 3 counts OCCURRENCES; format 2 counted MATCHING LINES. Two
# identifiers on one line were one tick in format 2, so a second one
# could be added to an existing line without any number moving. The
# version moved because the numbers mean something different, not
# because the layout changed -- reading a format-2 file with this
# parser would understate every count that has such a line.
20 shardpilot/client.lua
2 shardpilot/crash.lua
13 shardpilot/crash/client.lua
6 shardpilot/crash/dump.lua
4 shardpilot/experiments.lua
4 shardpilot/remote_config.lua
2 shardpilot/schema_revision.lua
1 shardpilot/sdk.lua
1 shardpilot/storage.lua
1 shardpilot/transport.lua
9 test/test_crash.lua
4 test/test_experiments.lua
2 test/test_remote_config.lua
28 test/test_sdk.lua
